My NixOS flake config. Running on a Lenovo ThinkPad T480s. Manages system configuration and home environment via home-manager.

Flake-based, so the full system is reproducible from a single `nixos-rebuild switch --flake .`.
